package database;

/**
 * Contains constants used in queries:
 * MIN - to get tuples containing the minimum value of a given attribute
 * MAX - to get tuples containing the maximum value of a given attribute
 */
public enum QUERY_TYPE {
    MIN, MAX
}
